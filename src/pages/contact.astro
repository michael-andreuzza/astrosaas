---
import BaseLayout from "../layouts/BaseLayout.astro";
import ContactForm from "../components/ContactForm.astro";
---

<BaseLayout>
  <section class="relative overflow-hidden bg-base-50/50 py-12">
    <div
      class="absolute inset-0 bg-[radial-gradient(40%_128px_at_50%_0%,theme(backgroundColor.accent.100/50%)_0%,transparent_100%)] -z-10"
    >
    </div>
    <div class="px-8 mx-auto md:px-12 lg:px-24 max-w-7xl">
      <div class="text-center">
        <h1
          class="gradient-underline text-4xl font-semibold tracking-tight text-black inline-block"
        >
          Contact Us
        </h1>
      </div>
      <p class="mt-4 text-lg text-center text-gray-600">
        Have questions? Send us a message and we'll get back to you.
      </p>
      <div class="mt-12 max-w-xl mx-auto">
        <ContactForm />
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Style for the gradient underline */
  .gradient-underline::after {
    content: "";
    display: block;
    width: 70%; /* Adjust width of the underline */
    height: 1px;
    background: linear-gradient(
      to right,
      transparent,
      currentColor,
      transparent
    );
    margin: 0.25rem auto 0; /* Adjust spacing (top margin) and center */
  }
</style>
